# Conditional Statements:-

Types:-
a. if-else (else if, nested if)
   Note:- else is optional.

b. switch

Switch properties:-
a. Cases can be in any order.
b. Nested switch (switch inside switch) is allowed.