# First C Program
To compile and execute use below command in terminal.
Step1 - gcc File name with extension. eg- gcc Hello.c (To invoke GCC and compile the code in a file.)
Step2 - ./a.exe (To Run the code in windows)